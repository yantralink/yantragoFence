package com.yantrago.gateway.service;

/**
 * Dashcam service — business logic for dashcam device management.
 *
 * This is the gateway-side interface referenced by JT808TcpServer.
 * Phase 12 will implement this with proper service logic.
 *
 * Currently a placeholder — the JT808TcpServer imports this but does not
 * inject it into its constructor. It will be used when dashcam streaming
 * management is added.
 */
public interface DashcamService {
    // Methods will be added in Phase 12 when dashcam streaming management is implemented
}
