# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: YantraGO Dashboards
    orgId: 1
    folder: YantraGO
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
